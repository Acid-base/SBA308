
// Function to format results from index.js
const formatResults  = results  => {
// 1. Convert the results to an array
// 2. Iterate over the array and format each result
// 3.  Return the formatted array
const formattedResults = results.map(result => {
    // Format the result as needed
    return result;
});
return formattedResults;
};

module.exports = { formatResults };
